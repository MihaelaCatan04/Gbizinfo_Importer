<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.importer.mapper.ImportFailureMapper">

    <select id="getFailureCount" resultType="int">
        SELECT COALESCE(attempts, 0)
        FROM import_failures
        WHERE entry_name = #{entryName}
          AND transaction_date = #{date}
    </select>

    <insert id="recordFailure">
        INSERT INTO import_failures (entry_name, transaction_date, attempts, last_error, last_attempted_at)
        VALUES (#{entryName}, #{date}, 1, #{error}, now())
        ON CONFLICT (entry_name, transaction_date) DO UPDATE
            SET attempts          = import_failures.attempts + 1,
                last_error        = #{error},
                last_attempted_at = now()
    </insert>

    <delete id="deleteFailure">
        DELETE
        FROM import_failures
        WHERE entry_name = #{entryName}
          AND transaction_date = #{date}
    </delete>

</mapper>